# -*- coding: utf-8 -*-
"""
Table Validator — 配置常量

定义真假表格分类和 LLM 验证的阈值。
"""

# ============================================================
# 真假表格分类阈值
# ============================================================

CLASSIFIER_CONFIG = {
    # ① 至少一列纯数值（排除首行表头后，该列数值占比 ≥ 此阈值）
    "numeric_column_ratio": 0.70,

    # ② 目录页排除：某列 > 该比例的值完全相同 → 判定为目录页
    "toc_duplicate_ratio": 0.60,

    # ③ 最少数据行（排除首行表头后）
    "min_data_rows": 3,

    # ④ 最少列数
    "min_columns": 2,
}

# 目录页关键词（任一列含这些词 ≥ 50% → 目录页）
TOC_KEYWORDS = ["目", "录", "目录", "Contents", "CONTENTS", "Table of Contents"]

# 非表格文本指纹——如果某行 > 50% 的格子含这些模式，怀疑该行是"段落混入"
NON_TABLE_PATTERNS = [
    "备注：", "注：", "说明：", "注释：", "附注：",
    "Note:", "Notes:", "Remarks:",
    "资料来源：", "数据来源：", "Source:",
    "单位：", "Unit:",
    "（续）", "(续)", "(Continued)",
    "未经审计", "经审计", "审计报告",
    "页码", "Page",
]

# ============================================================
# LLM 验证配置
# ============================================================

LLM_CONFIG = {
    # 最大并发（串行调用，此处仅占位）
    "max_concurrent": 1,

    # 单页数据最大字符数（超出则截断 full_text）
    "max_full_text_chars": 6000,

    # 单表数据最大行数（超出则只传首尾行）
    "max_table_rows_send": 40,

    # 请求超时（秒）
    "timeout_sec": 120,

    # 温度
    "temperature": 0.1,

    # 最大输出 token
    "max_output_tokens": 4000,
}

# ============================================================
# 验证缓存
# ============================================================

VALIDATION_CACHE_VERSION = 1
